<div class="restaurants">
  <h1>Tous nos restaurants</h1>

  <p class="text-right">
    <%= link_to("Votre restaurant n'est pas listé ?", new_restaurant_path, class: "btn btn-success") %>
  </p>
  <%- @restaurants.each do |restaurant| -%>
    <section class="row restaurant">
      <div class="col-sm-4">
        <%= cl_image_tag(restaurant.photo.path, width: 300, height: 200, crop: :fill, class: "picture") %>
      </div>
      <div class="col-sm-8">
        <h2><%= link_to(restaurant.name, restaurant_path(restaurant)) %></h2>
        <p><%= restaurant.city %></p>
      </div>
    </section>
  <% end %>
</div>
